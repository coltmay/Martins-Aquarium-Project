//Function that appends built fishHTMLPopulate into the inner HTML of the target.
export const fishListHTML = () => {
    
    //Locates positon of HTML to insert list into and assigns to variable.
    const DOMElement = document.querySelector(".fishList");
    
}